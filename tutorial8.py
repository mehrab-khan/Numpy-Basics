#Aggregation (Sum, Mean, Max, etc.)
import numpy as np
a = np.array([[1,2],[3,4]])
print(a.sum()) #Returns all elements sum
print(a.mean()) #Returns গড়
print(a.max()) #Returns Max num
print(a.min()) #Returns Min num